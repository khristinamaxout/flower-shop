# Optional: connect repo at https://app.netlify.com for auto-deploy
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  GITHUB_PAGES = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
